# 微表情识别项目环境依赖 (Requirements)

# --- 核心深度学习框架 ---
# 建议安装包含 CUDA 支持的版本以利用 GPU 加速
torch>=2.0.0
torchvision>=0.15.0

# --- 数据处理与标注解析 ---
numpy>=1.20.0
# 用于解析 CASME2.xlsx 和结果导出
pandas>=1.3.0
# pandas 读取 Excel 文件所需的引擎
openpyxl>=3.0.0

# --- 图像与视频处理 ---
# 核心图像处理
Pillow>=9.0.0
# 基础计算机视觉库
opencv-python>=4.5.0
# 包含 TV-L1 高级光流算法 (项目核心特征)
opencv-contrib-python>=4.5.0
# 可选：提供 3D ResNet 预训练权重加载支持
pytorchvideo>=0.1.5

# --- 机器学习与评估指标 ---
# 用于计算 UF1, UAR, 混淆矩阵以及 LOSO 划分
scikit-learn>=1.0.0

# --- 可视化 ---
matplotlib>=3.5.0
# 用于绘制美观的混淆矩阵热力图
seaborn>=0.11.0

# --- 常用安装命令参考 ---
# 1. 安装 PyTorch (针对您的 RTX 3070, 建议安装支持 CUDA 12.1 或 11.8 的版本):
# 推荐命令 (CUDA 12.1):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# 2. 安装其余依赖:
# pip install -r requirements.txt